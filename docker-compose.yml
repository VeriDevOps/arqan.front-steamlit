services:
  arqan.front:
    image: veridevops/arqan.streamlit-front:migration
    environment:
      - ARQAN_BACK_URL=${ARQAN_BACK_URL}
    ports:
      - "8501:8501"
    deploy:
      resources:
        limits:
          memory: 200M
